import SignInCard from "@/components/entry/SignInCard";
import React from "react";

const page = () => {
  return (
    <section className="max-h-screen">
      <SignInCard />
    </section>
  );
};

export default page;
